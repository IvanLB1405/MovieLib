<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para la API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieLib"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- Main Activity - Pantalla principal -->
        <activity
            android:name="com.movielib.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MovieLib">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Search Activity - Búsqueda de películas -->
        <activity
            android:name="com.movielib.SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.MovieLib" />

        <!-- Movie Detail Activity - Detalles de película -->
        <activity
            android:name="com.movielib.MovieDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.MovieLib" />

        <!-- Library Activity - Biblioteca personal -->
        <activity
            android:name="com.movielib.LibraryActivity"
            android:exported="false"
            android:theme="@style/Theme.MovieLib" />

        <!-- API Test Activity - Para pruebas de desarrollo -->
        <activity
            android:name="com.movielib.app.ApiTestActivity"
            android:exported="false" />

    </application>
</manifest>